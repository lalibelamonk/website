//components for routes
// import compositions from 'path/to/compositions';

export const routes = [
    {
       path: '/compositions',
        exact: true,
        private: true,
        component: Test,
        label: 'Dashboard'
    }
]